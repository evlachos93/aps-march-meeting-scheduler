{
  "name": "aps-talk-scheduler",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "dev:api": "npm --workspace @aps/api run dev",
    "stop:api": "kill-port 8787",
    "dev:web": "npm --workspace @aps/web run dev",
    "dev:scraper": "npm --workspace @aps/scraper run dev",
    "normalize:talk-weekdays": "node scripts/set-talk-weekdays.js",
    "generate:schedule": "npm --workspace @aps/scraper run build && npm --workspace @aps/scraper run schedule",
    "generate:sessions": "npm --workspace @aps/scraper run build && npm --workspace @aps/scraper run sessions",
    "generate:sessions-table": "npm --workspace @aps/scraper run build && npm --workspace @aps/scraper run sessions:table"
  },
  "devDependencies": {
    "kill-port": "^2.0.1"
  }
}
